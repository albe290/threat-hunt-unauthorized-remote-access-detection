index=<endpoint_index> sourcetype=<process_sourcetype> earliest=-30d "AnyDesk.exe"
| spath user
| stats count min(_time) as firstSeen max(_time) as lastSeen values(host) as hosts values(process_executable) as executable by user
| convert ctime(firstSeen) ctime(lastSeen)
| sort - count
